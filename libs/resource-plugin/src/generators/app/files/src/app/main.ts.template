import { <%= pascalCase(name) -%>AppModule } from './<%= name -%>-app.module.ts';
import { bootstrapBaseWebApp } from '@saas-buildkit/bootstrap';

void bootstrapBaseWebApp(<%= pascalCase(name) -%>AppModule);
