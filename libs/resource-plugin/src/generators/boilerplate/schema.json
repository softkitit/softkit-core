{
  "$schema": "http://json-schema.org/schema",
  "$id": "Boilerplate",
  "title": "",
  "type": "object",
  "properties": {
    "appFolder": {
      "type": "string",
      "description": "The folder name where the boilerplate will be created",
      "x-prompt": "What folder name would you like to use for the boilerplate?"
    },
    "companyName": {
      "type": "string",
      "description": "The name of the company associated with this boilerplate",
      "x-prompt": "Enter the company name to customize the 'name' field in package.json:"
    },
    "tag": {
      "type": "string",
      "default": "latest",
      "description": "The git tag to clone. Defaults to the latest tag if not specified",
      "x-prompt": "Enter the git tag to clone:"
    },
    "repository": {
      "type": "string",
      "default": "https://github.com/softkitit/softkit-nestjs-boilerplate.git",
      "description": "The URL of the git repository to be cloned",
      "x-prompt": "Enter the URL of the git repository to clone:"
    }
  },
  "required": [
    "appFolder",
    "companyName"
  ]
}
