jobsConfig:
  prefix: test
  jobs:
    - name: fake-job-queue
  systemJobs:
    jobs:
      - name: fake-system-job-queue
        cron: '*/1 * * * * *'
        jobData:
          version: 1
          executeForMillis: 900

