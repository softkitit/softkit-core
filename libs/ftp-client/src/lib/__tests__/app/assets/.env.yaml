defaultFTPClientConfig:
  host: ${TEST_FTP_SERVER_HOST}
  port: ${TEST_FTP_SERVER_PORT}
  user: ${TEST_FTP_SERVER_USERNAME}
  password: ${TEST_FTP_SERVER_PASSWORD}
  secure: false
  timeout: 4000
  connectOnStartup: true

customTokenFTPClientConfig:
  host: ${TEST_FTP_SERVER_HOST}
  port: ${TEST_FTP_SERVER_PORT}
  user: ${TEST_FTP_SERVER_USERNAME}
  password: ${TEST_FTP_SERVER_PASSWORD}
  secure: false
  connectOnStartup: true

noAutoConnectFTPClientConfig:
  host: ${TEST_FTP_SERVER_HOST}
  port: ${TEST_FTP_SERVER_PORT}
  user: ${TEST_FTP_SERVER_USERNAME}
  password: ${TEST_FTP_SERVER_PASSWORD}
  secure: false
  connectOnStartup: false

