swagger:
  title: Dev Application
  swaggerPath: /swagger
  enabled: true
  description: 'This is a test dev application'
  version: 1.0.0
  contactName: ${CONTACT_NAME:-}
  contactEmail: ${CONTACT_EMAIL:-example@example.com}
  contactUrl: ${CONTACT_URL:-https://example.com}
  servers:
    - { url: 'http://localhost:9999}', description: 'local server' }
