redisConfig:
  config:
    - url: "${TEST_REDIS_HOST}:${TEST_REDIS_PORT}"
      connectionName: "tests"
redisLockConfig:
  driftFactor: 0.02
  retryCount: 3
  retryDelay: 300
  retryJitter: 300
  automaticExtensionThreshold: 600
  defaultDecoratorLockDuration: 1200
