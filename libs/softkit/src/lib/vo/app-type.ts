export enum BackendPackageType {
  WEB = 'web',
  PROCESSOR = 'processor',
  LIBRARY = 'library',
}

export enum FrontendPackageType {
  WEB = 'web',
  CHROME_PLUGIN = 'chrome-plugin',
  LIBRARY = 'library',
  LANDING_PAGE = 'landing',
}
